import { MarkSearchPipe } from './mark-search.pipe';

describe('MarkSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
